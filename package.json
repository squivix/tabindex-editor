{
  "name": "tabindex-editor",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Edit and override the keyboard tab order of any web page (browser extension + userscript).",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "repository": { "type": "git", "url": "https://github.com/squivix/tabindex-editor.git" },
  "scripts": {
    "test": "node --test 'test/**/*.test.js'",
    "build": "./build.sh"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
